{
    "AWSTemplateFormatVersion": "2010-09-09",
    "Description": "Use TemplateURL. Templates are stored in S3. The root stack that contains all stacks needed for the app.",
    
    "Resources": {
        "Database": {
            "Type": "AWS::CloudFormation::Stack",
            "Properties": {
                "Parameters": {
                    "TableName": {
                        "Fn::Sub": "${AWS::StackName}-dynamodb-table"
                    }
                },
                "TemplateURL": "https://s3.amazonaws.com/test-template-bucket-wed15may2019/DatabaseStack.json"
            }
        }
    },

    "Outputs": {

    }
}